================================================================
PROJECT HANDOFF — แอปบันทึกรายรับ-รายจ่ายส่วนตัว
Version: v7  |  Updated: 2026-09-12
================================================================

[0] วิธีใช้ไฟล์นี้
----------------------------------------------------------------
แนบไฟล์นี้เป็นข้อความแรกของทุกแชทใหม่ แล้วบอกว่า
"อ่าน handoff v7 แล้วทำงานต่อจากงานค้างข้อ X"
เมื่อจบงานแต่ละก้อน ให้ขอ AI ร่าง handoff เวอร์ชันถัดไป แล้วเซฟเป็นไฟล์ใหม่
(อย่าเซฟทับ — เก็บเวอร์ชันเก่าไว้ย้อนดูเหตุผลการตัดสินใจ)

** ข้อควรระวังในการสื่อสารกับ AI **
เวลาส่งโค้ดให้ AI ตรวจ ให้พิมพ์ชื่อไฟล์กำกับไว้บรรทัดแรกเสมอ เช่น
  # main.py
เพราะถ้าไม่มี AI อาจเดาชื่อไฟล์ผิดจากเนื้อหาข้างใน และตรวจผิดไฟล์


[1] TECH STACK
----------------------------------------------------------------
Platform   : Pythonista (iPhone / iPad)
Language   : Python
UI         : ui module (native) + ui.WebView สำหรับตาราง HTML
Database   : SQLite
Editor     : VS Code (แก้โค้ด) / Pythonista (รันจริง)


[2] โครงสร้างไฟล์
----------------------------------------------------------------
main.py             : entry point + เลือก/สร้าง DB + เรียก ensure_schema()
                      *** เสร็จสมบูรณ์ (verify แล้วใน v7) ***
chanafunction.py    : create_new_file() + สร้างตารางเริ่มต้น
                      *** เสร็จสมบูรณ์ (แก้ return path แล้วใน v7) ***
db_manager.py       : จัดการฐานข้อมูล + TableManagerView (เพิ่ม/แก้/ลบ/แสดงข้อมูล)
                      + ensure_schema() / ensure_column()
                      *** เสร็จสมบูรณ์ (verify แล้วใน v7) ***
income_entry.py     : ฟอร์มบันทึกรายรับ (v2.1 — เสร็จสมบูรณ์)
expense_entry.py    : ฟอร์มบันทึกรายจ่าย (v3 — เสร็จสมบูรณ์)
data_viewer.py      : แสดงข้อมูล read-only ผ่าน WebView
                      *** เสร็จสมบูรณ์ (ยังปรับหน้าตาได้ถ้าต้องการ — งานค้างข้อ 2) ***
monthly_summary.py  : สรุปรายเดือน (กรอกเดือน+ปี แล้วแสดงสรุป)
                      *** เสร็จสมบูรณ์ (verify แล้วใน v7 — ไม่ต้องแก้อะไร) ***
yearly_summary.py   : สรุปรายปี (กรอกปี แล้วแสดงสรุปครบ 12 เดือน)
                      *** เสร็จสมบูรณ์ (ปรับ flow + แก้บั๊กใน v7) ***


[3] โครงสร้างฐานข้อมูล
----------------------------------------------------------------
Lookup tables:
  category_income, category_expense, payment_type, detail_master

Transaction tables:
  income  : date, year, month, detail_id(FK,nullable), detail_text,
            category_id, amount, note
  expense : เหมือน income + payment_type_id

detail_id เป็น NULL/0 ได้ กรณีผู้ใช้พิมพ์รายการเองโดยไม่เลือกจาก master
schema ปัจจุบันครบแล้ว ไม่ต้อง alter เพิ่ม

ensure_schema() ใน db_manager.py ใช้ CREATE TABLE IF NOT EXISTS
+ ensure_column() สำหรับ detail_text จึงปลอดภัยกับ DB เก่าและ DB ใหม่
ยืนยันแล้วใน v7 ว่าฟังก์ชันนี้มีจริงและชื่อตรงกับที่ main.py เรียกใช้


[4] การตัดสินใจออกแบบ + เหตุผล  ** อ่านก่อนแก้โค้ด **
----------------------------------------------------------------
4.1 Snapshot detail_text
    เก็บข้อความจริงลงตารางธุรกรรมเลย ไม่ JOIN detail_master ตอนอ่าน
    WHY: ข้อมูลเก่าไม่เปลี่ยนตามการแก้ชื่อใน master
         + ผู้ใช้พิมพ์เองได้โดยไม่ต้องเพิ่มเข้า master ก่อน
    >> อย่าเปลี่ยนไปใช้ JOIN ล้วน
    >> ยืนยันแล้วว่า monthly_summary.py / yearly_summary.py ไม่แตะ
       field นี้เลย (สรุปแค่ระดับหมวดหมู่) จึงไม่มีความเสี่ยงเรื่องนี้

4.2 Portable Clinic Category
    จับคู่หมวด "คลินิก" ด้วยชื่อ String ไม่ใช่ ID
    WHY: ย้ายไฟล์ DB ข้ามเครื่องแล้วยังทำงานถูก แม้ ID ไม่ตรงกัน
    >> อย่า optimize ไปใช้ ID
    >> ยืนยันแล้วว่า monthly_summary.py / yearly_summary.py ใช้ string
       compare (CLINIC_INCOME_NAME / CLINIC_EXPENSE_NAME) ถูกต้องทั้งคู่

4.3 Thai Display via WebView
    ใช้ HTML Table แทน monospace text
    WHY: ฟอนต์ไทยบน iOS ความกว้างไม่คงที่ จัดคอลัมน์ด้วย space ไม่ตรง

4.4 PickerPopup auto_close
      - auto_close=False : "รายละเอียด" (detail_master)
        เลือกแล้วเอาไปใส่ช่องค้นหาให้แก้ต่อได้ ไม่ปิด popup ทันที
      - auto_close=True  : "หมวดหมู่" และ "ประเภทการชำระ"
        เลือกแล้วปิดทันที
    >> ทำ picker ใหม่ที่ไหน ให้เลือก auto_close ตามลักษณะข้อมูล

4.5 Table-driven Schema ใน db_manager.py
    ใช้ list TABLES เป็นศูนย์กลางนิยามตาราง (name / title / columns)
    ทุกฟังก์ชัน (insert/update/fetch/form) generate จาก metadata นี้
    WHY: เพิ่มตารางใหม่ทำได้โดยแก้จุดเดียว ไม่ต้องเขียนฟอร์มใหม่
    >> เพิ่มตารางใหม่ = เพิ่ม dict ใน TABLES + เพิ่ม method ใน DataViewer
       + เพิ่ม mapping ใน show_data()

4.6 show_data() ใช้ dict mapping ไปยัง DataViewer
    WARNING: dict viewer_actions ถูกสร้างตอนเรียก show_data()
    ถ้า DataViewer ขาด method ใดใน mapping จะเกิด AttributeError ทันที
    แม้ผู้ใช้ยังไม่ได้เลือกตารางนั้น
    >> เพิ่ม key ใน mapping เมื่อไหร่ ต้องมี method จริงใน data_viewer.py ด้วยเสมอ

4.7 สรุปรายปี (yearly_summary.py) ใช้ flow "กรอกปีก่อน" (ตัดสินใจใน v7)
    เดิมเคยออกแบบให้ดึงข้อมูลทุกปีมาแสดงพร้อมกันหน้าเดียว แต่พบปัญหาว่า
    header โชว์ได้แค่ปีเดียวทั้งที่ตารางมีหลายปีปนกัน
    ทางเลือกที่พิจารณา:
      (ก) โชว์ช่วงปีในหัวข้อ เช่น "2568 – 2569"
      (ข) ทำฟอร์มให้กรอกปีก่อน คล้าย monthly_summary.py  << เลือกทางนี้
    WHY เลือก (ข): สอดคล้อง pattern เดียวกับ monthly_summary.py ทำให้
         ผู้ใช้คุ้นชิน ไม่ต้องสองมาตรฐาน + query เร็วขึ้นเพราะ filter
         WHERE year = ? ตั้งแต่ต้น ไม่ต้องดึงทั้งฐานข้อมูลมากรองทีหลัง
    ผลคือ _fetch_data() รับ (db_path, year) และ _build_html() รับ year
    ตรงๆ ไม่ต้องเดาปีจาก data อีกต่อไป
    ตารางสรุปจะแสดงครบ 12 เดือนเสมอ แม้เดือนไหนไม่มีข้อมูล (ยอด 0.00)
    >> ถ้าจะทำหน้าสรุปใหม่ในอนาคต (เช่น สรุปหลายปีเทียบกัน) ให้เพิ่มเป็น
       ฟีเจอร์ใหม่แยกต่างหาก ไม่ใช่เปลี่ยน yearly_summary.py กลับไปแบบเดิม


[5] ปัญหาที่เคยเจอ + วิธีแก้ (อย่าย้อนรอยเดิม)
----------------------------------------------------------------
- ฟอนต์ไทยจัดคอลัมน์เพี้ยน           -> ใช้ HTML Table
- สระอำ U+0E33 นับความกว้างผิด       -> เพิ่ม exception ให้ width = 1
- AttributeError INPUT_ROUNDED_RECT  -> เข้าถึงผ่าน getattr()
- layout() ถูกเรียกซ้ำ                -> ใช้ _built guard flag
- จอ iPad ขนาดต่างกัน                 -> คำนวณขนาดแบบ dynamic
- AttributeError 'FormX' has no attribute 'end_editing'
                                      -> ใช้ ui.end_editing()
- ScrollView ไม่มี send_subview_to_back
                                      -> add subview ตัวที่ต้องการให้อยู่ล่างสุดก่อน
                                         (ลำดับ add = z-index)
- ฟังก์ชัน helper ที่คืนค่าไม่ครบทุก branch (implicit None)
                                      -> ทำให้เรียกใช้ผลลัพธ์ต่อไม่ได้
                                         ตรวจด้วยการอ่าน "possibly unbound"
                                         warning จาก linter เป็นสัญญาณเตือน
- CSS ใน string ธรรมดา (ไม่ใช่ f-string) ใช้ {{ }} ผิดที่
                                      -> ถ้าไม่ใช่ f-string ต้องใช้ { } เดี่ยว
                                         เท่านั้น ไม่งั้น CSS parse ผิดที่ browser
                                         (เจอใน _build_empty_html ของ
                                         yearly_summary.py แก้แล้วใน v7)


[6] สิ่งที่ทำเสร็จแล้ว (สะสมจาก v3-v7)
----------------------------------------------------------------
[x] income_entry.py — Keyboard-avoidance เต็มรูปแบบ
[x] income_entry.py — PickerPopup auto_close logic
[x] expense_entry.py — พอร์ต keyboard-avoidance + auto_close ครบ 3 ฟิลด์
[x] ทดสอบฟอร์มรายรับ/รายจ่ายใช้งานจริงแล้ว — ผู้ใช้ยืนยันว่าใช้ได้ดี
[x] db_manager.py — show_data() รองรับครบทุกตารางใน TABLES
[x] data_viewer.py — มี show_payment_type() ใช้ _build_html_lookup()

--- ใหม่ใน v7 ---
[x] main.py — verify ครบ 3 จุด (db_path / ensure_schema / เมนูหลัก)
      >> งานค้างข้อ 1 ของ v6 ปิดแล้ว
[x] main.py — แก้ flow gap: create_new_file() ทั้งจาก main() ตอนเปิดแอป
      ครั้งแรก และจาก settings_action() ตอนกดเมนู "..." ตอนนี้เข้า
      show_main_menu() ต่อได้ทันที ไม่ต้องปิดแอปเปิดใหม่
[x] main.py — เรียก db_manager.ensure_schema(path) ก่อนเข้าเมนูหลักแล้ว
      ทุกเส้นทาง (เลือกไฟล์เก่า / สร้างไฟล์ใหม่)
[x] chanafunction.py — create_new_file() แก้ให้ return db_path เมื่อสำเร็จ
      และ return None เมื่อยกเลิก/error (เดิมไม่ return อะไรเลย)
[x] db_manager.py — ยืนยันแล้วว่ามี ensure_schema()/ensure_column() ครบ
      ตรงกับที่ main.py เรียกใช้ ไม่มีปัญหาเรื่องชื่อฟังก์ชันไม่ตรงกัน
[x] monthly_summary.py — ตรวจครบ 3 จุด (snapshot / payment_type /
      clinic string-match) ผ่านทุกจุด ไม่ต้องแก้อะไร
      >> งานค้างข้อ 2 ของ v6 (ส่วน monthly) ปิดแล้ว
[x] yearly_summary.py — ตรวจพบและแก้บั๊ก _build_empty_html() ที่ไม่ใช่
      f-string แต่ใช้ {{ }} ผิด ทำให้ CSS ของหน้า "ยังไม่มีข้อมูล" เพี้ยน
[x] yearly_summary.py — ปรับ flow ใหม่ทั้งหมดจาก "แสดงทุกปีอัตโนมัติ"
      เป็น "กรอกปีก่อนแล้วค่อยแสดง" (เพิ่ม _YearInputView คล้าย
      _SummaryView ใน monthly_summary.py) ตามเหตุผลใน [4.7]
      >> งานค้างข้อ 2 ของ v6 (ส่วน yearly) ปิดแล้ว
      >> ปัญหา "header โชว์ปีเดียวทั้งที่มีหลายปี" จาก v6 หมดไปโดยธรรมชาติ
         เพราะตอนนี้ query ถูก filter ทีละปีตั้งแต่ต้น


[7] งานค้าง (เรียงตามลำดับที่แนะนำ)
----------------------------------------------------------------
[ ] 1. (ไม่เร่งด่วน) ปรับหน้าตา data_viewer.py ให้สวยขึ้น
       ผู้ใช้ระบุว่า "ทำงานได้ แต่หน้าตายังไม่สวย"
       ทำทีหลังได้ ไม่กระทบ logic
       ไอเดีย: ดึงสไตล์ dark theme + card layout จาก monthly_summary.py /
       yearly_summary.py มาปรับใช้ ให้ธีมสีสอดคล้องกันทั้งแอป

[ ] 2. (optional) เพิ่ม comment วิธีเรียกใช้ show_payment_type()
       ที่หัวไฟล์ data_viewer.py ให้เอกสารตรงกับโค้ดจริง

[ ] 3. (optional) ทดสอบ end-to-end ทั้ง flow จริงบน Pythonista
       (สร้างไฟล์ใหม่ -> กรอกรายรับ/รายจ่ายสองสามรายการ -> เปิดสรุป
       รายเดือน/รายปี -> ตรวจว่าตัวเลขตรงกัน) ยังไม่เคยมีรายงานผลทดสอบ
       เต็ม flow อย่างเป็นทางการ แม้แต่ละไฟล์จะ verify แยกแล้ว

[ ] 4. (optional, พิจารณาในอนาคต) ถ้าต้องการดูสรุปเทียบหลายปีในหน้าเดียว
       (เช่น กราฟแนวโน้ม 3 ปีย้อนหลัง) ให้ทำเป็นไฟล์ใหม่แยกต่างหาก
       ตามที่ตกลงไว้ใน [4.7] ไม่ควรย้อนกลับไปแก้ yearly_summary.py
       ให้ดึงหลายปีพร้อมกันแบบเดิม


[8] คำแนะนำสำหรับแชทถัดไป
----------------------------------------------------------------
- เปิดแชทใหม่ด้วย: "อ่าน handoff v7 แล้วเริ่มงานข้อ 1 (ปรับหน้าตา data_viewer.py)"
  แล้วแนบโค้ด data_viewer.py พร้อมพิมพ์ # data_viewer.py กำกับบรรทัดแรก
- ถ้าต้องทำ picker ใหม่ ให้ดู [4.4] ก่อนตัดสินใจ auto_close
- ถ้าจะเพิ่มตารางใหม่เข้าระบบ ให้ดู [4.5] และ [4.6] ควบคู่กัน
- ถ้าจะทำหน้าสรุปใหม่ ให้ดู [4.7] เรื่องแนวทาง "กรอกก่อนแสดง" ที่เลือกใช้
- ถ้าเขียน HTML string ใหม่ (ไม่ว่าไฟล์ไหน) ให้เช็คให้แน่ใจว่าถ้าเป็น
  f-string ต้องใช้ {{ }} เสมอ ถ้าไม่ใช่ f-string ห้ามใช้ {{ }} เด็ดขาด
  (ดู [5] บรรทัดสุดท้าย เป็นบทเรียนจาก v7)
- ถ้าเจอปัญหาคีย์บอร์ด/scroll ในไฟล์ใหม่ ให้ copy pattern จาก
  income_entry.py / expense_entry.py แทนเขียนใหม่จากศูนย์
- ถ้าเจอ warning "possibly unbound" จาก linter ในไฟล์ไหนก็ตาม ให้ตรวจว่า
  มีฟังก์ชันที่ไม่ return ค่าครบทุก branch หรือมีตัวแปรที่ assign เฉพาะ
  บาง if-branch แล้วถูกใช้นอก branch นั้นหรือไม่ (บทเรียนจาก main.py ใน v7)

================================================================
END OF HANDOFF v7
================================================================